const admin = require('firebase-admin');
const serviceAccount = require('./firebase-adminsdk.json'); // assuming it exists, or just use default

// Just use `firebase firestore:query` if it's available or something else.
