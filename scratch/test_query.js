const admin = require('firebase-admin');
const serviceAccount = require('../../firebase-adminsdk.json'); // We probably don't have this, let's just write a test nextjs route or check browser console if possible.
