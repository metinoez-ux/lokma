const admin = require("firebase-admin");

// Initialize Firebase Admin with default credentials
// (Assuming you are logged in using `firebase login` or have google application credentials set)
// However, to use admin SDK, you need service account or ADC.
// Alternatively, I can use the `firebase database:get` or `firebase firestore:get` from CLI?
